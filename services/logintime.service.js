var User = require('../models/user.model');
var moment = require('moment');

exports.saveLoginTime = function (loginTime) {
    if (!!loginTime) {
        // Login time is correct and is defined
    } else {

    }
};